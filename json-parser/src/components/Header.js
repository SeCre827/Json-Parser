import React from "react";
import classes from "./Header.module.css";

const Header = () => {
  return (
    <div className={classes.header}>Simple React app to parse json object</div>
  );
};

export default Header;
